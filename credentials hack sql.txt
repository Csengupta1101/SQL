importing the os module would be the first criteria here . Once we have done that , let's move to our system environment variables and create new user variables. 
There will be two user variables that we will be creating , one for user name and one for password.
Once the same is done , we can simply call the functions in our notebook to get the details accessed with OS module.